import * as selectors from "./product/selectors";
import * as useCases from "./product/use-cases";

// export const selectors = {
//   selectAllProducts(state: RootState) {
//     return state;
//   },
// };

export { useCases, selectors };
